@model CraftsMarket.Web.ViewModels.Products.AllFromCategoryViewModel
@{
    this.ViewData["Title"] = $"Products in {@Model.CategoryName}:";
}

<h1>@this.ViewData["Title"]</h1>

@foreach (var product in @Model.Products)
{
    <partial name="_ProductPartial" model="@product"/>
}